# A class that models an experiment. The class should be able to read a CSV file (using pandas), and create the experiment.
# The experiment is constituted by a list of OderIDs and a duration for each odor
# THe experiment also has a attribute for the name, which is the same as the filename
# The experiment should also be able to be created dynamically frm the UI
